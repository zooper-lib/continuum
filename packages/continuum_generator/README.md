# Continuum Generator

Code generator for the continuum event sourcing library.

## Usage

Add `continuum_generator` to your `dev_dependencies`:

```yaml
dev_dependencies:
  build_runner: ^2.4.0
  continuum_generator: ^0.1.0
```

Run the generator:

```bash
dart run build_runner build
```
